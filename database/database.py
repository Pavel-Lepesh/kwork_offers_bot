from collections import defaultdict


#  Это тестовый вариант, не воспринимать всерьез
root_offers_set = defaultdict(set)
root_res_values = defaultdict(list)
root_categories = defaultdict(list)
process_status: dict[int, bool] = dict()
